services:
  # Angular Frontend (Render Static Site)
  - type: web
    name: student-management-system-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist/sms-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
